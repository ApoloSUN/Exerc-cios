package main

import "fmt"

func main() {
	var (
		n int64
	)

	fmt.Println("Digite um valor: ")
	fmt.Scanln(n)

	if n%2 == 0 {
		fmt.Println("O número", n, "é par")
	} else {
		fmt.Println("O número", n, "é ímpar")
	}
}
