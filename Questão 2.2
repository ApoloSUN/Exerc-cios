package main

import "fmt"

func main() {
	var (
		n int64
	)

	fmt.Println("Digite um valor: ")
	fmt.Scanln(n)

	if n >= 1 {
		fmt.Println("O número", n, "é positivo")
	} else if n < 0 {
		fmt.Println("O número", n, "é negativo")
	} else {
		fmt.Println("O número", n, "é nulo")
	}
}
